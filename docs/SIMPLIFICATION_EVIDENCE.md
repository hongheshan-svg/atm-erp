# 精简实施与验收证据

## 全部业务评审整改（2026-09-09）

- 按 `docs/REVIEW_REMEDIATION.md` 实施，同物料多单元BOM、变更影响入口、采购退回修改/明细到货计划、隔离到货处理、结算凭据追溯、合同补充协议、剩余成本估算、工作台直达及概览折叠均复用原模块。新增0007向前迁移保留原合同金额、明细交期、历史预算调整人，并创建不可变补充协议记录；不清库、不覆盖原付款，不新增服务或角色。
- 后端最终155项通过（平台31、业务103、并发21），Ruff、Django及迁移无漂移检查通过，日志 `/private/tmp/lean-review-backend-final.log`。包含1000行BOM固定4次查询、2000笔结算导出至多8次查询，以及隔离品并发释放和补充协议并发版本冲突。规模验证为独立测试库，不宣称生产吞吐量。
- `npm ci`、lint、typecheck、26项Vitest及build通过；日志 `/private/tmp/lean-review-npm-ci.log`、`lean-review-lint.log`、`lean-review-typecheck.log`、`lean-review-unit-final.log`、`lean-review-build.log`。依赖安装提示可选fsevents脚本未授权，未额外开放脚本，构建及验收不受影响。
- 首轮新增桌面页面链1项通过（44.6秒），日志 `/private/tmp/lean-review-browser-smoke.log`。随后完整回归暴露旧用例依赖默认任务标签，以及关闭工作台直达详情后刷新仍重新打开的定位参数问题：用例显式进入任务标签，产品关闭详情时移除定位参数；未通过强制点击或跳过断言规避。
- 最终完整桌面10项/移动视口10项，共20项通过（6.4分钟），日志 `/private/tmp/lean-review-browser-final.log`。追加付款流水加载完成断言后专项2项通过（1.6分钟），日志 `/private/tmp/lean-review-browser-assertions.log`；截图等待真实银行流水出现后保存，已查看桌面及移动实际截图，可视化目录 `review-remediation/desktop.png`、`review-remediation/mobile.png`。101个运行源码及构建文件与本地SHA-256一致，摘要 `/private/tmp/lean-review-parity.json`。
- 最新数据备份 `/private/tmp/atm-erp-lean-review-20260909.zip`，恢复至全新项目 `atm-erp-lean-review-restore-20260909`（18354）。29张表逐行摘要、45个附件SHA-256全部一致，源数据未变；非空恢复被拒绝且目标不变，重复安装保留全部表与附件。日志 `/private/tmp/lean-review-restore.log`，摘要 `/private/tmp/lean-review-restore-evidence.json`。
- 恢复实例使用production、DEBUG=false、login=10/min。原账号登录、销售/项目/付款数据与私有附件下载一致，匿名附件下载被拒绝；一次正确登录后，连续错误登录的9次401之后返回429，实际限流生效。摘要 `/private/tmp/lean-review-production-evidence.json`；源18350仍为开发模式，其他已有实例未操作。
- 本轮部署仅使用18350隔离实例，三个服务健康，日志 `/private/tmp/lean-review-deploy-final.log`。全部检查基于模拟业务数据；企业实际订单试用、手机真机与远端CI不属于已完成证据，未提交/推送/发布版本。

## 六角色真实页面完整复验（2026-09-09）

- 按用户要求重新执行已部署18350隔离实例的完整浏览器套件。管理员从用户管理新建经理、采购、仓库、财务、成员账号，各账号使用独立浏览器上下文登录；业务写入通过真实表单，接口读取仅用于核对结果和越权断言。
- 链路覆盖需求编辑后重报价格、签约及合同编号、BOM导入与勾选采购、超预算批准、付款冲销重录、分次收货/取消余量/退款、设计装配调试及工时更正、缺料禁止发货、领料后分批发货安装验收、免费和收费售后、费用登记、未结清禁止结项、结清后结项与重开。另跑品牌/单元/类别多选、导入导出、报表授权撤销、附件鉴权、分页侧栏及导航。
- 测试前只读逐文件SHA-256核对Docker后端源码及前端构建产物共99个文件，与当前工作区完全一致。本轮仅复验，未修改业务实现，未重跑无改动的后端单元测试。
- 最终桌面9项、移动视口9项，共18项全部通过（4.9分钟），日志 `/private/tmp/lean-role-recheck-browser.log`。成本核对材料500、人工400、费用50、合计950元，预算950元、已承诺采购0元；结项成功后按测试步骤重开。已查看本轮桌面项目截图，可视化目录 `role-recheck/desktop-chain.png`。移动覆盖为浏览器视口，不是手机真机；本轮未提交、推送或运行远端CI。

## BOM 分类筛选与多选采购（2026-09-09）

- 最终桌面/移动完整18项通过（4.9分钟），日志 `/private/tmp/lean-bom-selection-browser-final.log`；专项2项通过（15.6秒），日志 `/private/tmp/lean-bom-selection-browser-smoke.log`。真实页面导入13行分类BOM，三个维度组合多选、跨页勾选3行并保存，接口核对采购草稿恰好包含这3行；原六角色签约至结算链同样通过。已查看桌面及移动实际选料截图，位于本机可视化目录 `bom-selection/desktop.png`、`bom-selection/mobile.png`。代码未提交、推送或发布，未运行远端CI；移动验证为浏览器视口，不代表手机真机。
- 物料新增 brand/part_type（标准件、非标件或未分类），项目BOM行新增 assembly_unit（设备功能单元）。品牌与类别从物料投影，单元只在BOM维护，旧记录不推测分类。迁移0006增加三个字段，不增加模块、单元表或多层BOM，保留同项目同物料一行约束。
- 采购列表和项目BOM共用选料弹窗：品牌、单元、类别支持多值组合筛选，搜索编码/名称/规格，跨页及换筛选保留已选项，可全选筛选结果。无缺口或停用物料禁选，缺口列固定右侧。进入采购表单时刷新需求，只带勾选项，保存仍走原from_demand/bom_line、项目锁及缺口校验，不自动提交审批，不按品牌新增单独台账。
- 物料维护、BOM维护、导入预览与导出同步字段；旧物料四列/旧BOM三列模板兼容，旧BOM导入保留原单元。单元变更审计并更新版本，品牌/类别清空受支持。
- 后端完整144项通过（平台31、业务94、并发19），日志 `/private/tmp/lean-bom-selection-backend.log`；覆盖分类维护与非法值、事实投影、仅所选行下单、重复/超额拒绝、错误BOM关联、旧模板与单元版本。前端lint/typecheck、25项Vitest及构建通过。独立18350 Docker迁移及部署完成，三个服务健康，日志 `/private/tmp/lean-bom-selection-deploy.log`。
- 浏览器专项首轮发现测试直接点击ElSelect内部输入会被占位元素覆盖；检查同时修正全局原生输入样式影响ElSelect内部输入的问题。筛选验收改为等待控件启用后键盘展开并选择选项，不强制点击、不跳过断言。最终浏览器结果完成后另记。

## 业务表格导入导出（2026-09-09）

- 最终桌面及移动完整16项通过（4.6分钟），日志 `/private/tmp/lean-transfer-browser-final.log`，包含原六角色业务链、报表授权撤销、分页侧栏、附件和新导入导出。已查看桌面及移动预览截图，保存在本机可视化目录 `transfers/desktop.png` 与 `transfers/mobile.png`。代码未提交、推送或发布，未运行远端CI；手机覆盖为浏览器移动视口，不是手机真机。
- 12类批量新增复用原服务：物料、往来单位、销售草稿、项目、采购草稿、任务、期初、领料、费用、正常收付款、工时及发货；BOM 保留原版本校验导入。支持 CSV/XLSX 模板，5MB/1000行上限；采购按文件分组号合单。预览在事务内校验并回滚，确认使用绑定用户和模块的30分钟签名凭据重新校验，整批原子保存及 ActionReceipt 防重复。不导入派生余额或覆盖历史，不自动签约审批。
- 业务列表统一鉴权导出全部筛选结果，20000行上限，复用原过滤查询与序列化敏感金额范围；采购逐行展开，报表含项目与合计。CSV/XLSX 单元格防公式执行，编码和金额按文本保持精度，附件只导出元数据。
- 后端完整141项通过（平台31、业务91、并发19），日志 `/private/tmp/lean-transfer-backend-final.log`；覆盖预览零持久写入、编号计数回滚、坏行整批回滚、确认时数据变化、角色及项目隔离、过期/篡改/跨用户凭据、并发重复确认、库存与金融上限、公式和大于当前页的全量导出。Ruff/Django/迁移检查通过。
- 前端 lint/typecheck、24项 Vitest及构建通过；独立18350 Docker已部署，日志 `/private/tmp/lean-transfer-deploy.log`。桌面/移动真实文件专项2项通过（3.9秒）：下载Excel模板、CSV预览及确认12条、默认每页10条仍导出全部12条、报表Excel下载。初次测试数量定位同时匹配预览说明与分页总数，收窄定位后通过，未改变产品逻辑绕过断言。完整浏览器回归结果待运行完成后补记。

## 统一分页、长页面侧栏与开发限流（2026-09-09）

- 侧栏文案二次修正：外层侧栏随整页拉伸，品牌和菜单使用内部 sticky 容器，底部文案通过自动间距落在整页左下角，距底部24px；不再只延伸背景。已部署18350，lint/typecheck/build通过，桌面及移动导航/分页专项4项通过（7.3秒），日志 `/private/tmp/lean-sidebar-footer-browser.log`。已查看实际完整截图 `compact-layout/sidebar-footer.png`，断言验证文案距整页底部24px及滚动后菜单可见。本轮仅前端布局改动，未重复后端业务测试。
- 最终桌面/移动完整14项通过（4.6分钟），日志 `/private/tmp/lean-layout-browser-final.log`，包含六角色业务链、报表授权撤销、附件及分页布局。桌面截图保存在本机可视化目录 `compact-layout/desktop.png`。当前代码未提交、推送或发布，未执行远端 CI。
- 通用业务列表、经营报表、BOM 需求及导入预览共用每页10/20/50/100条设置，默认10条，浏览器保存并跨页面复用，改大小回第一页；表格高度限制560px，更多行在表格内滚动。BOM 分页只处理展示，确认导入仍保留完整预览行。报表接口新增 page_size，汇总不随当前页或条数变化。
- 桌面布局在整个侧栏列延伸深色背景，菜单仍随滚动保持可见；窄屏继续顶部导航，主体保持浅色。样式作用于所有业务页面，未通过裁剪截图处理问题。
- APP_ENVIRONMENT=development 关闭原登录/刷新限流，production及未设置时保留10次/分钟，拼写错误拒绝启动。安装器/Compose 默认 production，发布约束写入 AGENTS；本地独立18350实例明确设置 LEAN_ENVIRONMENT=development，实测 APP_ENVIRONMENT=development、DEBUG=False。发布Compose配置单独核对为 production、DEBUG=false，未修改生产数据或关闭鉴权。
- 后端完整134项通过：平台31、业务85、并发18；日志 `/private/tmp/lean-layout-backend.log`，覆盖两种环境限流行为、默认生产配置、环境值错误与分页边界。前端 lint/typecheck、24项 Vitest 和构建通过，持久化单测使用浏览器存储桩；本机 Node 的 localStorage 实验性提示不属于应用浏览器警告。
- 部署日志 `/private/tmp/lean-layout-deploy.log`，三个服务健康。桌面/移动分页布局专项2项通过（2.3秒），验证跨页面与重载保存、换页大小后汇总不变、表格高度和桌面整页背景覆盖。已检查最终桌面短页截图。PowerShell安装器测试尝试因本机缓存镜像为amd64、模拟器崩溃而未完成，临时容器已停止；不将此结果记为Windows实机验证。

## 管理员与总经理经营报表（2026-09-09，v1.0.0 之后）

- 按用户要求新增第十个入口“经营报表”，为固定只读页面。保留六角色，总经理使用 manager 账号并由管理员显式开启 management_reports，默认为 false；管理员默认可看，其他项目经理与财务无报表权限。授权/撤销留审计，JWT 每次请求读取现有授权，撤销后立即拒绝接口，前端重新加载后隐藏入口。
- 汇总筛选项目的累计签约合同额、实际成本、在途采购、待收待付、逾期收付、两种方向的待退款及预算/交付风险；支持项目名称/编号、状态、风险筛选和每页20条明细，汇总包含全部筛选结果。数据只读取既有业务事实，不新增报表账或收入/完工利润推算。成本与预算投影改为共享批量查询，25 个项目的报表聚合查询不超过8次，避免逐项目数据库查询。
- 最终后端全量检查通过：平台29、业务85、并发18，共132项，包含原采购/库存/收付款/预算/编号回归；日志 `/private/tmp/lean-reports-backend-final.log`。验证授权与即时撤销、项目预算口径一致、付款冲销不改成本、取消项目退款不抵消正向余额，以及筛选、分页、空数据与软删除。初次报表测试夹具保存旧项目对象覆盖了刚设置的预算，已限制仅更新交付日期后重跑通过。
- 前端 lint、typecheck、23项 Vitest、生产构建通过；金额展示直接格式化十进制字符串，保留大金额分位精度。最终部署至独立 Docker 测试实例18350，三个服务健康，日志 `/private/tmp/lean-reports-deploy-final.log`。
- 桌面及移动报表专项4项通过（13.6秒），真实表单创建总经理测试账号、开启授权、查看并筛选报表、撤销授权，随后接口403及导航消失均已验证。已检查桌面报表截图。完整套件首次连续测试触发原有登录10次/分钟限制，日志 `/private/tmp/lean-reports-browser-final.log`；测试登录现按429的 Retry-After 等待再提交，并断言页面限流提示，仅允许具体登录URL的429控制台日志，不修改生产限流或放宽业务断言。
- 最终桌面/移动完整12项全部通过（5.2分钟），日志 `/private/tmp/lean-reports-browser-verified.log`，包含原六角色签约至结算链、编号配置、报表授权撤销与导航。桌面报表截图保存在本机可视化目录 `management-reports/desktop.png`。前端测试辅助调整后 lint/typecheck 再次通过，生产代码与最终部署一致。本轮未提交、推送、发布或运行远端CI；总经理真实账号由管理员在用户管理中指定授权，没有批量赋予现有项目经理报表权限。

## 自定义编码（2026-09-09，v1.0.0 之后）

- 继续在 `codex/project-budget-control` 补齐六类公共自动编号配置：前缀、日期、流水位数、重置周期。仅管理员可改，要求版本和原因，复用幂等与审计；配置变化不回退当前计数，日期切换在编号行锁内重置，历史业务编号不改写。默认仍为原前缀加六位流水，不新增编码规则引擎。
- 新建物料可手填编码，留空使用公共生成器；手工和自动生成共用物料编号锁，生成器跳过已有编码，停用或软删除记录仍占用编码。签约可填写独立合同编号，合同编号唯一且可搜索，签约后不可覆盖；历史合同保留空值，不补造标识。合同编号只在销售维护。
- 最终后端全量检查通过：平台 28、业务 81、并发 18，共 127 项。验证按年/月/日重置、并发跨周期取号、手工与自动物料编码争用、重复合同编号并发签约、角色授权、规则版本及幂等回放；Ruff、Django、迁移漂移检查通过。日志 `/private/tmp/lean-codes-backend-final.log`。
- 前端 lint、typecheck、21 项 Vitest 和构建通过。原有“编码不可配置”的测试预期已按新行为更新，同时保留无权角色和不支持资源的限制断言。通过安装脚本部署至独立 Docker 测试实例 18350，三个服务健康；日志 `/private/tmp/lean-codes-deploy.log`。
- 桌面和移动视口浏览器共 10 项全部通过（4.5 分钟），日志 `/private/tmp/lean-codes-browser.log`。真实表单修改编号规则、留空新建物料并断言新编号格式，随后恢复原配置；完整六角色链使用手工物料编码完成 BOM/采购，并签约录入及查看合同编号。已查看实际桌面编号规则弹窗和销售合同编号列表，截图保存在本机可视化目录 `custom-codes`。代码未提交、推送或发布，未运行远端 CI 或手机真机测试。

## 轻量项目预算管控（2026-09-09，v1.0.0 之后）

- 在 `codex/project-budget-control` 增加项目材料、人工、费用预算及预算修订号。预算修改复用项目动作、幂等凭据和审计；实际成本复用领料、工时快照、费用与退货价差，不新增成本台账。预算金额仅向管理员、经理和财务开放，修改及例外批准沿用管理员、经理权限。
- 已承诺成本只计已批准未收货采购，采用累计金额差的分币口径；人工和费用暂不建立预计承诺。另以累计采购净额检查材料预算，收货不释放采购额度，取消和退货通过原应付抵减释放，支付不影响成本。共享库存无法精确分配各项目已收未领材料，本表不声称预测完工成本。
- 超预算普通批准在服务端拒绝。例外批准要求明确勾选确认、填写原因并提交当前检查快照；项目锁防止两张采购并发占用同一额度，预算或业务数据变化使旧快照失效。批准不自动增加预算，审计保留批准人、原因和当时成本。未设置预算的旧项目提示后继续原流程；明确设置零预算会执行拦截。
- `bash scripts/precheck-tests.sh --all` 通过：平台 25、业务 79、并发 15，共 119 项；Ruff、Django check、迁移漂移与统一测试注册检查均通过。日志 `/private/tmp/lean-budget-backend.log`。覆盖预算版本、幂等回放、敏感金额隔离、零预算、并发审批、过期快照，以及收货/退货/费用/付款成本口径。
- 前端 lint、typecheck、20 项 Vitest、生产构建通过。通过安装脚本部署至独立 Docker 测试实例 18350，迁移 0004 已应用，三个服务健康；日志 `/private/tmp/lean-budget-deploy.log`。
- 最终桌面与移动视口浏览器套件 10 项全部通过（4.3 分钟），日志 `/private/tmp/lean-budget-browser-final.log`。六角色真实表单链包括设置预算、采购超预算确认、调整预算及后续收货、生产和结算，最终核对预算 950.00、实际 950.00、在途 0.00、累计采购净额 500.00。首轮发现测试修改预算后未回采购列表，修正测试导航后重跑完整套件通过。
- 已查看最终桌面预算对比表和超预算审批弹窗；截图位于本机可视化目录 `budget-control`。复用规则已同步仓库与本机 `lean-erp-validation` 技能。当前代码未提交、推送或发布；本轮没有执行远端 CI 或手机真机键盘测试。

## UI 视觉整理（2026-09-09）

- 在 `codex/ui-polish-20260909` 保留深蓝侧栏、蓝色主操作及白色卡片，统一圆角、边框、留白、表格行距、按钮和弹窗层级。侧栏桌面滚动保持可见，顶部展示当前模块和账户；窄屏继续显示九个导航入口。
- 工作台按既有权限和待办数据展示分类图标、数量、说明及模块入口，空状态补充上下文，不新增统计账或业务数据。刷新时显示加载状态并防止连续重复请求。
- 前端 lint、typecheck、19 项 Vitest、构建通过。部署至原 18350 独立实例，日志 `/private/tmp/lean-ui-polish-deploy.log`；桌面/移动浏览器完整 10 项通过（4.2 分钟），日志 `/private/tmp/lean-ui-polish-browser.log`。包括六角色完整链、导航、短视口长表单及低频设置键盘操作。
- 已检查实际桌面工作台、销售列表和新建销售弹窗，以及 390 宽工作台截图。截图保存在本机可视化目录 `ui-polish`。未修改后端、数据库、金额或权限规则；本轮不重复运行后端测试。当前 UI 改动纳入 v1.0.0 发布，远端验证以关联 PR 的 CI 为准。

## 当前版本最终安装与恢复验收（2026-09-09）

- 从当前 18350 实例备份至 `/private/tmp/atm-erp-lean-sales-20260909.zip`，恢复到全新 `atm-erp-lean-sales-restore-20260909`（18352），未使用已有 18351 恢复实例或其卷。28 张表的记录数/逐行内容摘要、16 个附件 SHA-256 均一致，源数据未变。原管理员密码登录两端后，23 张销售单、项目和鉴权下载结果再次比对一致。
- 停止目标应用后再次恢复，确实到达非空库检查并拒绝；目标数据未变，随后重启目标。源环境重复安装后，两端全部表和附件仍与快照一致。验证脚本 `/private/tmp/lean-sales-verify-restore.py`，日志 `/private/tmp/lean-sales-restore.log`，摘要 `/private/tmp/lean-sales-restore-evidence.json`；这些产物不含明文环境密钥。
- 当前镜像另在全新 `atm-erp-lean-final-fresh-20260909`（18353）用安装脚本自动生成配置并启动三个健康服务，随机管理员登录成功，销售和项目均为空。日志 `/private/tmp/lean-sales-fresh-install.log`。18350、18352 和 18353 保留供复核，未清理旧系统或任何既有卷。
- 本轮前端 lint/typecheck、19 项 Vitest 及构建再次通过，主块 356.48 kB，无体积警告。部署中 63 个运行 Python 文件、20 个前端产物与本地逐文件 SHA-256 相同。首次比较把本地测试目录计入，按 `.dockerignore` 排除后完全相等，未修改运行代码来消除差异。
- 已再次查看最新完整项目桌面截图及销售截图，界面风格一致；技能与已安装副本再次 `cmp` 一致。后端最终 111 项和桌面 5 项的有效证据见下一节，本轮只改验收文档。
- 整体要求逐项结论见 `ERP_OPTIMIZATION_STATUS.md`；本地目标完成。以下各节为按时间保留的阶段证据，其中“仍待验收”的描述反映当时状态，不覆盖本节结论。未提交/推送，未执行远端 CI、Windows Docker 实机或手机真机键盘验证。

## 销售拆分最终桌面复验与输入契约（2026-09-09）

- 当前独立销售入口、模块页面/动作/服务边界保持 `MODULE_BOUNDARIES.md` 的划分。合同只在销售维护，签约事务交接项目与应收；未新增独立部署服务。
- 平台密码接口的非对象 JSON 已先复现 500，再修复为 400；回归验证原密码与审计不变。BOM 修订强制合法版本，缺失/错误版本拒绝，同一快照并发修改只有一个成功。
- 完整后端检查 111 项通过（平台 25、业务 72、并发 14），日志 `/private/tmp/platform-contract-final.log`；格式、模型与迁移检查通过。
- 已更新 18350 独立 Docker 实例，三个服务健康、数据保留。最终桌面 5 项全部通过（2.1 分钟），日志 `/private/tmp/sales-module-final-desktop.log`，包括六角色销售→采购→生产→分批交付→售后→结算，以及附件、登录、九入口导航和设置键盘操作。
- 已查看本次桌面销售截图，侧栏销售独立可见、可跳转执行项目，保留现有配色布局。本轮未重跑移动仿真；当前销售 schema 的备份恢复仍待独立验收，不将桌面业务验证等同于整个 ERP 验收完成。

## 业务模块纠错与状态一致性审查（2026-09-09）

- 已逐项读取采购、库存、款项、BOM、项目纠错、工时/交付、工作台与附件服务，记录见 `docs/MODULE_REVIEW.md`。
- 发现付款冲销、工时更正后原记录仍显示重复操作按钮。接口增加只读 `reversed_by`，关联预加载避免逐行查库；前端共用流水状态显示“有效/已冲销/冲销记录”，原记录保留但隐藏重复按钮。后端拒绝重复动作的校验保持不变，成员工时金额过滤回归通过。清理财务表单中的验收分支和项目表单中的付款冲销分支。
- 发现独立创建且没有销售合同的项目在交付后仍可改设备数量/质保，已由服务端交付记录保护并同步隐藏相关编辑字段；执行说明仍可修改。新增回归明确使用无销售合同项目。
- 恢复任务完成后的工时补录入口，与后端允许 open/done 的行为一致；仅执行人或经理可见，取消任务不提供入口。
- 后端完整检查通过 108 项（平台 24、业务 71、并发 13），日志 `/private/tmp/ledger-state-backend-final.log`。初次检查因一处测试格式未通过，在修正后重跑完整入口通过。前端 lint/typecheck、19 项行为测试、构建通过。
- 部署日志 `/private/tmp/ledger-state-deploy.log`。完整桌面/移动浏览器 10 项全部通过（4.2 分钟），日志 `/private/tmp/ledger-state-browser.log`。实际验证付款冲销→重新付款、先完成任务再补录工时，以及冲销后原流水状态与按钮消失；六角色交付、售后、结算和成本 950.00 核对通过。已查看最终桌面项目截图，原记录与冲销记录均保留。

## 低频设置收纳与编辑分支清理（2026-09-09）

- 设置默认展示账户和用户管理，公司资料、编号规则、操作审计各自折叠。共用 `DeferredSection` 使用原生 details/summary，首次展开才挂载内容；关闭后保留已加载组件与状态，重复展开不重复请求。继续使用现有卡片配色，补充键盘焦点样式。
- 清理模块拆分后基础资料编辑中不可达的公司/用户分支、设置编辑中的空路由映射和多余字段处理。无效动作明确拒绝；用户编辑密码可留空，启用字段只出现一次。
- 前端 lint、typecheck、16 项行为测试与生产构建通过，无体积警告。后端源码未改，本轮不重复运行相同后端测试；上轮 107 项结果仍对应当前后端源码。
- 最终部署日志 `/private/tmp/settings-fold-final-deploy.log`。桌面 5 项（含六角色完整链）全部通过，移动端设置定向测试通过，日志 `/private/tmp/settings-fold-browser.log`。验证初始无三类低频请求，桌面 Enter/空格及移动 tap 展开成功，关闭后不可见，重新展开每类请求仍只有一次。已查看最终桌面折叠截图，保留原卡片与侧栏风格。

## 销售更正流程（2026-09-09）

- 审查发现销售单创建后无法更正需求、数量等基础信息，只能取消重建。补充签约前编辑，复用新建校验；修改后返回需求状态、清除当前报价，审计保留前后内容与旧报价，必须重新报价才能签约。已签约或取消的销售不可编辑。
- 编辑请求携带打开表单时的更新时间，事务锁内检查版本。两个客户端同时改同一版本只有一个成功，过时编辑拒绝。相同请求回放不会撤销后续重新报价状态；无实际变化或非法字段拒绝且不改动报价。
- 历史迁移的未签约项目在签约交接时接收更正后的销售需求、设备和质保等信息，保留项目编号、成员和执行负责人。销售编辑时不提前修改项目。
- 独立 PostgreSQL 完整检查通过：平台 24、业务 70、并发 13，共 107 项；日志 `/private/tmp/sales-edit-backend.log`。前端 lint、typecheck、15 项行为测试和生产构建通过。部署日志 `/private/tmp/sales-edit-deploy.log`，仍使用 18350 独立实例，没有数据库迁移或清库。
- 桌面浏览器 4 项全部通过（2.0 分钟），日志 `/private/tmp/sales-edit-desktop.log`。通过真实表单先报价，再编辑需求，核对状态回到 draft、报价为零且未建项目，重新报价签约后跑完六角色采购、生产、分批交付、售后与结算；附件、登录及导航回归同时通过。本轮未重跑移动仿真，新增路径以桌面结果为证据。
- 上游编辑失效与旧版本冲突的验收规则已写入仓库验证技能，并同步到本机已安装副本，逐字节一致；覆盖前先核对原文件无其他修改，原副本备份在 `/private/tmp/lean-erp-validation-before-sales-edit.md`。

本轮不作为整体 ERP 目标完成证明；剩余检查在 `docs/ERP_OPTIMIZATION_STATUS.md`，包括低频设置收纳和当前版本各模块审查收口。

## 销售独立与模块拆分（2026-09-09）

用户明确要求销售单独拿出来、每个模块解耦，本轮据此将范围改为九个入口；先前八入口约束不再适用于当前导航。边界见 `docs/MODULE_BOUNDARIES.md`。

- 销售使用独立 `SalesOrder` 和 `/api/business/sales/`，报价时不建立项目。签约在同一事务中调用项目建档与应收入口，返回关联项目；项目不再提供报价、签约动作。合同金额、报价与日期只在销售维护，项目读取只读投影。
- `0003_sales_order` 对当前 Lean 表执行向前迁移，没有清库或绕过保护。升级 18350 隔离实例前保存 14 个项目的逐字段快照；升级后核对全部项目原字段不变、三个商业字段逐条对应销售记录。日志输出为 `Verified unchanged project rows and migrated contract facts: 14`，验证脚本 `/private/tmp/sales-migration-evidence.py`，基线 `/private/tmp/sales-migration-before.json`。不代表支持旧版全量 ERP 数据库迁移。
- 删除通用 `WorkspacePage.vue` 和单体业务 View 文件，各入口独立页面，后端接口按领域拆分。前端 `src/modules/` 分别承担销售、项目、BOM、采购、库存、收付款、基础资料、设置与附件动作；`business.ts` 只做资源分发。模块专属表单放在模块内，通用控件、权限与请求复用。目录请求按需要加载，基础资料和用户维护不请求无关业务目录。
- 后端完整检查通过 103 项：平台 24、业务 67、并发 12。新验证包括销售报价不创建项目、同键签约回放、不同键重复及并发签约拒绝、应收交接失败回滚项目与编号、财务只读与降权后回放拒绝、无销售的内部项目创建。日志 `/private/tmp/sales-backend-final.log`；Ruff、模型检查、迁移漂移和统一测试清单检查通过。
- 前端 lint、typecheck、14 项行为测试与生产构建通过；模块测试额外验证销售表单不依赖项目/库存可用性，以及项目不显示签约动作。构建无体积警告。
- 第一轮桌面与移动仿真完整 8 项浏览器测试通过（3.8 分钟），日志 `/private/tmp/sales-browser.log`。最终镜像补齐销售只读明细，部署日志 `/private/tmp/sales-final-deploy.log`；最终 8 项复验全部通过（3.9 分钟），日志 `/private/tmp/sales-final-browser.log`。验证销售签约与只读合同明细、执行项目链接、后续六角色完整链路、九入口导航、长表单和私有附件；没有非预期浏览器控制台错误。已查看最终桌面销售页与完整项目页截图，配色与布局保持原风格。

当前仍使用单一 Lean 应用和数据库，模块通过单据关联和明确服务调用协作；不是独立部署的微服务。桌面为主验收环境，移动使用 Chromium 仿真。未提交、推送或执行远端 CI。

## 异常流程与移动端续审（2026-09-09）

本轮保持八个入口与现有主题，使用同一 18350 隔离实例，未修改或清空旧系统数据。

- 手机导航原为横向滚动，后半入口不可直接看到；改为四列换行，保留原角色过滤、图标与配色。导航测试逐个验证八个入口完全在视口中并进入页面，手机项目使用触控 tap。
- 手机长表单改为内容独立滚动，标题与底部保存/取消保持可见。390×480 短视口测试在填写底部质保字段后仍可看到并操作按钮，已查看禁用过渡动画后的稳定截图。短视口模拟不等同于真机软键盘测试。
- 请求失败后表单滚回顶部错误提示并保留输入。组件回归验证网络错误后的输入、滚动位置、相同数据重试复用幂等键、修改数据换键、请求未结束时连续提交只发送一次。
- Playwright 现在分 desktop/mobile 两个项目；完整业务链中的五个独立角色 context 继承对应 viewport、isMobile、hasTouch，避免只把管理员页面缩窄却把其他角色留在桌面尺寸。手机链路实际完成签约、采购、领料、任务、交付、售后及结算。
- 新增后端异常回归：全额退款后不能直接冲销原付款，拒绝时不留 ActionReceipt 或新流水；先冲销退款、再冲销付款可恢复零余额，重复相同请求不增记录，原始正负流水均保留。独立 PostgreSQL 并发退款验证两个请求只有一个成功，净付款与应退余额为零。现有服务规则满足这些断言，本轮没有为通过测试改变退款政策。
- 后端完整检查通过：平台 24、业务 63、并发 11，共 98 项；日志 `/private/tmp/lean-exceptions-mobile-backend.log`。前端 lint/typecheck、11 项单测与生产构建通过，无体积警告。
- 第一轮桌面/手机共 8 项浏览器测试通过（3.7 分钟），日志 `/private/tmp/lean-exceptions-mobile-browser.log`。最后的错误提示滚动修复已重新构建部署，最终 8 项再次全部通过（3.8 分钟），无非预期控制台 error/warning 或 pageerror，日志 `/private/tmp/lean-exceptions-mobile-final-browser.log`。构建日志 `/private/tmp/lean-exceptions-mobile-final-build.log`。最终 lint、typecheck 与 diff 空白检查通过。
- 已检查手机完整链路截图和 390×480 长表单截图，导航八个入口完整换行，保存/取消固定在内容滚动区之外。截图路径分别为 `frontend/test-results/full-chain-六角色完成签约采购生产分批交付售后与结算-mobile/completed-chain-mobile.png` 和 `frontend/test-results/navigation-dialog-各入口可直接操作，长表单在短视口保留保存和取消-mobile/short-viewport-dialog.png`。初次短视口截图截到弹窗动画中间态，改用禁用过渡动画后的截图检查稳定布局，未据中间态修改产品样式。

仍有限制：浏览器使用 Chromium 移动仿真，不代表 iOS/Android 真机或全部软键盘行为已验证；本次退款异常组合不等同于穷尽全部金额/并发场景。整体优化目标未据此标记完成。

## 当前重构状态（2026-09-09，重新确认 Lean 范围后）

当前分支 `codex/lean-rebuild-20260909`。候选源码恢复自 Git 树 `20d3ce96308c0c2d8d108a3e3ab1adfb6d8503d3`，不是从远端获得的新版本。本文下方原有“本次验证结果”属于恢复的历史记录，不作为当前代码通过证明。

本次重新执行的后端完整入口通过：平台 24、业务 62、PostgreSQL 并发 10，共 96 项；Ruff、迁移漂移和测试清单检查通过。日志 `/private/tmp/lean-rebuild-backend-20260909.log`。前端 npm ci、lint、typecheck、9 项单测和生产构建通过，日志 `/private/tmp/lean-rebuild-frontend-20260909.log`；主块 827.86 kB，仍有体积警告。

新独立安装 `atm-erp-lean-rebuild-20260909` 已完成，端口 18350，未复用旧业务数据库或附件卷。初次日志 `/private/tmp/lean-rebuild-install-20260909.log`，最终镜像日志 `/private/tmp/lean-rebuild-install-final-20260909.log`。最终镜像只含 core/accounts/business，用户为 uid 10001 的 erp；实际进程仅 supervisord 管理 Daphne 与 Nginx，没有 Celery 或升级代理。

本次后续实测：

- Element Plus 改为实际使用的 13 个控件插件及根 ConfigProvider，保留中文配置和原 CSS。最大 JS 包由 827.86 kB 降至 270.47 kB（gzip 90.94 kB），未修改警告阈值，构建无体积警告；lint、typecheck、9 项单测再次通过。
- 最终镜像的 3 项浏览器测试全部通过，耗时 1.8 分钟，日志 `/private/tmp/lean-rebuild-browser-verified-20260909.log`。完整链路使用六角色实际表单，涵盖签约、BOM、分批采购收货、生产任务、领料、分批交付、售后和结算；附件匿名访问及成员接口隔离也有断言。
- 统一浏览器 fixture 捕获 pageerror、console error/warning。第一次加入时捕获两个负向用例产生的预期 409 网络日志；已限制为发货/结项两个具体 URL、状态和单次配额，响应与页面业务拒绝仍须断言，未全局忽略 4xx 或警告。
- 已查看 1440 像素桌面和 390 像素手机的项目详情截图，保留深色侧栏、蓝色操作和 Element Plus 表格页签。手机截图可见成本和表格，主导航及宽表格通过横向滚动；截图不能证明完整移动端操作链通过，仍列为未完成范围。
- `scripts/backup.py` 将本次测试库及附件恢复到独立 `atm-erp-lean-rebuild-restore-20260909`（18351）。逐表核对 27 张表的行数和排序后数据摘要，3 个附件 SHA-256 一致，备份未改变源数据。停止恢复实例后再次恢复，触发非空库拒绝，目标数据摘要不变，随后应用恢复健康。日志 `/private/tmp/lean-rebuild-restore-20260909.log`；备份 `/private/tmp/atm-erp-lean-rebuild-20260909.zip` 不含环境密钥。
- `skills/lean-erp-validation/SKILL.md` 已通过技能校验，并安装到本机同名技能目录。没有提交、推送或执行远端 CI。

可复用验收方法保存在 `skills/lean-erp-validation/SKILL.md`，引用现有契约、服务和统一测试清单；不把运行时代码移入技能。整体目标仍未完成：需继续逐模块检查权限/状态/金额不变量、超量与退款冲销组合、完整移动端操作及低频操作安排。Windows 实机安装和远端 CI 未执行。本次主链通过不代表已穷尽所有角色与异常组合。

基线：origin/main 的 8fd47308ec6540471131e6ed86ffd70e4a91753e。

远端版本有 197 个 Vue 页面、211 处路由定义。项目模块直接依赖生产模块，财务直接依赖 OA，不能只隐藏菜单而保留整套旧系统。

本次在 codex/lean-erp 分支继续，复用后端候选实现后重新验证，历史报告不视为当前测试结果。后端当前只保留 core、accounts、business；旧库保护是全新安装的前置约束。

## 三条可复用规则

1. 合同生成应收、采购批准生成应付、成本来自实际业务，禁止重复可编辑台账。
2. 采购、库存、收付款等操作先校验角色和项目范围，再按项目→单据→物料顺序加锁，锁内重读，事务和幂等回放一起提交。并发和失败路径必须有回归用例。
3. 后端测试目标唯一注册在 scripts/ci/backend_test_matrix.py；每个测试文件恰好归属一个阶段，CI 与本地共用。

## 本次验证结果（2026-09-09）

| 验收项 | 本轮证据 |
| --- | --- |
| 删除复杂模块与代码 | 后端 Python 文件由 619 减至 64，仅 core/accounts/business；前端 src 文件由 302 减至 22，八个入口由三个业务页面组件承载。OA、复杂生产、AI、升级代理、小程序目录无剩余运行文件。前端直接依赖只保留 Vue、Vue Router、Element Plus、图标和 Axios。 |
| 后端平台、完整业务链、并发 | `bash scripts/precheck-tests.sh --all` 成功：平台 24、业务 62、PostgreSQL 并发 10，共 96 项。均使用独立临时 PostgreSQL；测试容器和网络已清理。 |
| 统一检查入口 | 加入备份工具后运行 `bash scripts/precheck-tests.sh checks`，Ruff check/format、Django check、迁移无漂移、测试注册覆盖检查均通过。 |
| 前端行为与构建 | lint、typecheck、9 项 Vitest、Vite 构建通过。覆盖表单字段白名单、十进制余量、会话刷新并发与退出竞态、新建入口默认显示及权限隐藏。 |
| 实际三服务构建 | `install.sh` 从源码构建并启动独立 `atm-erp-lean-resume` 项目，新 PostgreSQL 和附件卷；三个服务健康，应用用户为 erp，实际进程仅 supervisord、Daphne 和 Nginx。API、前端经 Nginx 在 `127.0.0.1:18320` 提供服务。 |
| 六角色真实浏览器链路 | 最终构建上运行完整 Playwright 套件，3 项全部通过，耗时约 1.9 分钟。所有业务写入均通过真实界面；API 只用于读取结果验证。 |
| 业务验收细节 | 创建六角色账户、基础资料、项目与合同，CSV BOM 预览确认，缺料采购；部分收货、取消余量、预付退款、追加采购；设计→装配→调试和工时更正；领料不足拒绝发货，两批安装验收；免费与收费售后、售后领料、费用及结算；未结清拒绝结项，结清后结项再打开。 |
| 成本一致性 | 浏览器业务项目核对材料 500.00、人工 400.00、费用 50.00、退货价差 0.00，合计 950.00；重新打开不改变历史成本。 |
| 权限和附件 | 普通成员只见自己项目，采购、款项、成本接口均返回 403；仓管采购明细无单价。合同通过真实表单上传，鉴权下载内容一致，匿名下载 403，公开 media 路径 404。 |
| 桌面与移动显示 | 查看最终 1440 像素桌面和 390 像素移动截图；窄屏使用横向导航和表格滚动，成本与主要操作可见。截图保存在 frontend/test-results 的 full-chain 输出目录。 |
| 备份与空环境恢复 | `scripts/backup.py` 短暂停止源测试应用，保存数据库与附件，随后启动原容器。恢复至独立 `atm-erp-lean-restore-resume` 新项目和新卷。比对 12 类业务资源、项目成本及所有附件 SHA-256 均一致，原管理员密码可登录。 |
| 非空保护与重复安装 | 停止恢复测试应用后再次恢复，被非空库保护拒绝，随后原应用正常启动。源环境重跑安装和重建后，原项目与独立恢复快照仍一致，密码未重置。 |
| PowerShell 安装入口 | 官方原生 Linux ARM64 PowerShell 运行安装分派测试，验证随机凭据、自定义配置持久化、SkipBuild、正常 build、健康等待与重复安装保留配置，退出码 0。Windows 实机 Docker 安装未在此 Mac 上执行；已配置 Windows CI 分派测试。 |
| 文档和自动检查 | 根 README、AGENTS、前后端 README 与接口契约已更新；旧原生部署、升级、自动补丁、小程序、过时测试产物及发布镜像流水线已删除。新 CI 共用本地后端测试入口，并包含前端与正式构建浏览器测试。YAML 语法及 git diff --check 通过。代码尚未推送，未声称远端 CI 已运行。 |

## 验证中修复的实际问题

- 原始表单标签包含下拉选项文字：显式设置控件可访问名称。
- 项目状态控制新增入口时，Vue 将未传入的布尔参数默认设为 false：明确默认 true，并用真实组件回归验证角色和状态控制。
- 自定义镜像标签未持久化，维护命令可能转用默认镜像：安装配置保存 LEAN_IMAGE；备份预检镜像、禁止隐式拉取，并用 start 恢复原容器。备份完成前验证附件 tar 可读，恢复前校验哈希与路径。

## 适用边界

全新数据库安装，不提供旧数据库自动升级；旧业务库和用户原有配置未操作。当前浏览器证据来自独立测试安装，数据均为模拟数据。备份不包含环境密钥，配置应单独保管。

构建仍有 Element Plus 主块约 828 kB（gzip 264 kB）的体积提示；没有调高阈值来隐藏提示。Windows Docker 实机安装与远端 CI 运行不在本机验证结论内。
# 第二轮经营与操作整改验收（2026-09-09）

范围见 `OPERATIONAL_REVIEW_REMEDIATION.md`。沿用既有项目与资金流水，新增仅为缺失的业务事实：剩余材料估算、独立付款期限、本批交付配套快照、不可变凭证追加关联。没有引入权限树、可配置流程或新业务模块。

- 后端完整入口通过162项（平台31、业务110、并发21），含模型/迁移一致性与静态检查。新增用例覆盖材料估算缺省、付款期限独立、凭证幂等及越权拒绝、交付配套与退料、BOM预览无持久化、成本来源及待办分页。日志：`/private/tmp/lean-operational-backend-final.log`。
- 前端npm ci、lint、typecheck、26项单元测试、build通过；日志为`/private/tmp/lean-operational-{npm-ci,lint,typecheck,unit,build}.log`。保存保留页码并刷新自身列表，移动端抽屉导航及当前标签可见；新建、冲销保护仍保留。
- 浏览器20项覆盖通过：完整运行18项通过，新增预览专项2项修正等待后复测通过。桌面与移动完整六角色链通过；旧断言已改为禁止重复冲销而允许凭证补录。主运行日志：`/private/tmp/lean-operational-browser-acceptance.log`；专项日志：`/private/tmp/lean-operational-browser-preview.log`。新增专项包含BOM预览、后补凭证、剩余材料估算、成本追溯弹窗及当前标签可见断言。
- Docker使用既有隔离18350环境并保留数据升级，部署日志：`/private/tmp/lean-operational-deploy-final.log`。镜像比对排除.dockerignore中的测试文件，103个实际运行文件校验；最终源码核对记录：`/private/tmp/lean-operational-parity.json`。
- 运维检查用例通过，验证缺失、有效、过期/损坏归档及空间阈值；命令`python3 -m unittest discover -s scripts/tests -p test_ops_check.py`，日志`/private/tmp/lean-operational-ops-test.log`。新检查脚本验证归档内数据库/附件SHA256，但不能代替实际恢复演练。
- 实际归档`/private/tmp/lean-operational-backups/20260909.zip`恢复到独立18355环境：30张表和57个附件校验完全一致；非空目标拒绝恢复且数据不变，重复安装保留数据。日志`/private/tmp/lean-operational-restore.log`，逐表/附件证据`/private/tmp/lean-operational-restore-evidence.json`。运维脚本对该真实归档和18350健康接口返回ok=true，记录`/private/tmp/lean-operational-ops-check.json`。
- 已查看最终桌面报表、项目收付款和移动收付款截图；当前标签可见、首列金额摘要和紧凑报表主列可读。截图保存于`/Users/zhengshan/.codex/visualizations/2026/09/08/01a0832c-3411-7620-83b4-bdd8ceec3d12/operational-review/`。
- 尚未提供企业真实脱敏订单，试运行清单已准备；实际主机告警接收渠道未配置，不声称已发送外部告警。未提交、推送或发布版本。
# 三平台发布补充（2026-09-09）

## 七角色真实业务链最终回归（2026-09-10）

- 完整覆盖说明见 `REALISTIC_CHAIN_ACCEPTANCE.md`。销售经理独立建单、需求修订后重新报价、签约指定项目经理，继续同一个项目的采购、生产、分批交付、免费/收费售后、财务结清和结项；销售经理最终只读核对交付和回款。
- 桌面、移动端完整浏览器 26 项全部通过，无重试，日志 `/private/tmp/erp-goal-browser-all.log`；包含异常链、权限、附件、表格、页签和键盘，不再沿用先前 25/26 的未完成结论。截图位于 `/private/tmp/erp-goal-browser-results/`，已查看两种视口的销售结项回款页面。
- 业务断言核对合同 10,000 元及收费售后 300 元全部收齐，材料 500、人工 400、费用 50，总成本 950 元；在途承诺归零，欠款时结项拒绝，结清后成功，重新打开保留事实。
- 后端全量 179 项通过，日志 `/private/tmp/erp-goal-backend-final.log`。修复一处报表测试的固定到期日期在跨日后失效，保留当天到期不算逾期的边界断言；未更改正确的报表业务计算。前端 lint、typecheck、31 项单元测试和构建通过，git diff --check 通过。
- 隔离 18360 安装的 82 个后端文件及 28 个前端构建文件与当前工作区一致，哈希记录 `/private/tmp/erp-goal-runtime-hashes.json`、`/private/tmp/erp-goal-frontend-hashes.json`。本次保留功能分支未提交改动，不发布版本，不将模拟数据称为企业实单。

## 独立销售经理角色（2026-09-09）

- 按用户明确新增要求，固定角色扩展为七个；仅新增 sales_manager 枚举与数据库约束迁移，不增加 Role 表或权限树。保留现有账户与数据。
- 销售经理只查询、导出及操作本人负责的销售单；客户共享，供应商隔离，双用途客户只读。签约单独选择项目经理，交付与应收回款从原业务记录只读投影，不新增收款或成本台账；完整项目、采购、库存、财务写入和成本接口拒绝访问。
- 后端完整检查通过 174 项（平台 38、业务 115、并发 21），金额格式微调后业务 115 项复测通过。覆盖订单隔离、导出隔离、签约交接、回款投影、非法角色操作、订单转交与角色降权后的旧请求重放。日志 `/private/tmp/erp-sales-role-backend.log`、`/private/tmp/erp-sales-role-business-final.log`。
- 前端 lint/typecheck、30 项单元测试及部署构建通过。隔离服务 18360 使用显式测试口令，经管理员页面创建销售经理并登录，桌面/移动端完成客户新建、本人建单、报价、签约和回款只读验证；沿用测试数据库，不修改既有账号密码。
- 最终桌面/移动端专项 8 项通过，含新增销售经理业务链、管理员导航和总经理报表授权/撤销；日志 `/private/tmp/erp-sales-role-browser-acceptance.log`。已查看桌面和移动端截图。未发布新 tag，v1.2.0 原发布包不变。


### 三平台安装发布记录

- v1.0.0 / v1.1.0 业务源码保持原 tag；发布附件叠加独立可追溯安装器及说明，不改变业务、角色、schema guard 或数据库版本。
- Docker 保留三个服务与原安装器；原生应用使用 Daphne + Nginx，数据库/Redis 由部署者显式配置。Windows 不内置 Redis 7，需已有服务；包为联网安装包，不是离线镜像。
- 安装器配置不覆盖、生产环境强制、路径转义、服务失败阻止迁移、附件不公开的自动测试位于 scripts/tests/test_native_install.py。
- GitHub Platform release installers 对 macOS/Linux/Windows 验证脚本及依赖；Linux 对两版本真实原生进程执行首次/重复安装、登录、前端资源、匿名权限、附件拒绝及限流验证。Windows/macOS 完整真实数据库业务链不由这些检查替代。
- 运行及升级入口详见 docs/INSTALL_PLATFORMS.md。发布附件包含源码/安装器提交记录与 SHA256 校验清单。

## 销售与采购附件验证（2026-09-09）

- 统一 Document 增加互斥的销售/采购归属，保留原项目记录；项目列表动态汇总原关联，签约不复制文件。下载沿用鉴权接口，单据附件按源单权限读取，不能借“其他”分类绕过合同隔离。
- 后端完整检查 179 项通过（平台 38、业务 120、并发 21），含未签约上传、签约汇总、单文件幂等、转交/降权后回放拒绝、列表/导出/下载隔离、非法归属、取消后拒绝及补充协议附件来源校验。日志 `/private/tmp/erp-order-attachments-backend-final.log`。
- 前端 lint、typecheck、31 项单元测试及构建通过。隔离地址 `http://127.0.0.1:18360` 上，桌面/移动端原项目附件、新销售附件及采购附件业务专项 6 项通过，日志 `/private/tmp/erp-order-attachments-browser.log`。截图检查促成新弹窗的窄屏宽度修复，并增加可视边界断言。
- 当前部署已应用 `0009_document_owners` 前向迁移，保留已有数据与文件；部署日志 `/private/tmp/erp-order-attachments-deploy-final.log`。未发布新 tag。
- 自适应修复后的最终桌面/移动端专项 4 项全部通过，含真实角色上传下载、签约关联、采购合同与项目汇总、补充协议及凭证流程；弹窗边界和下载按钮视口断言通过。日志 `/private/tmp/erp-order-attachments-browser-final.log`；已查看最终桌面销售及窄屏采购截图，保存到当前任务 visualizations 目录。

## 模块页面拆分验证（2026-09-09）

- 设置、基础资料、库存、收付款和项目详情内的堆叠模块改为独立页签；预算与成本从项目概览移入专用入口。仍复用原接口、权限与派生数据，不新增业务台账。
- 页签支持链接直达、按用户记忆、首次打开加载和键盘激活。修复父子页签更新链接的竞争、切换触发重复请求，以及窄屏初次布局时当前标签未滚入可视区的问题。
- 前端 lint、typecheck、31 项单元测试和构建通过，单元测试包含父子导航及 Enter/空格激活回归。日志 `/private/tmp/erp-module-tabs-unit-final.log`，最终部署日志 `/private/tmp/erp-module-tabs-deploy-final.log`。
- 浏览器目标明确指定为隔离地址 `http://127.0.0.1:18360`，使用显式测试口令；本次未变更后端业务逻辑，未发布新 tag。
- 完整 26 项浏览器回归中 25 项通过，桌面 13 项全部通过；移动端项目刷新后标签不可见促成原生滚动修复，日志 `/private/tmp/erp-module-tabs-acceptance.log`。最终滚动专项 8 项中 7 项通过，包含真实触摸滑动、设置键盘和分页；移动端异常链在项目加载及附件预取处出现超时（接口有正常 200 响应，耗时接近或超过断言 5 秒），未计为通过。日志 `/private/tmp/erp-module-tabs-scroll-final.log`、`/private/tmp/erp-module-tabs-mobile-final.log`；不以局部通过宣称最终全套回归全绿。
- 最终只读窄屏核验已通过：从实际成本切换至项目收付流水、折叠概览、刷新，确认父子页签保持、选中标签处于视口且原银行流水可见。截图 `erp-project-tabs-mobile.png` 保存在当前任务 visualizations 目录；已检查桌面设置及项目、窄屏项目截图。

## ERP 在线升级验证（2026-09-09）

- 管理员左上角版本入口复用现有会话和固定角色；升级任务独立保留审计与幂等结果，不增加业务成本或库存事实。应用容器不访问 Docker socket，宿主机执行器只使用专用升级令牌。
- 后端完整检查通过 169 项（平台 38、业务 110、并发 21），覆盖非管理员拒绝、重复任务、版本/资产校验、执行器绑定及状态推进；日志 `/private/tmp/lean-ota-backend-final.log`。
- 前端 lint、typecheck、29 项单元测试和 build 通过；执行器 7 项及原生安装器 7 项单元测试通过。
- Docker 隔离升级实际完成安装包校验、停机、数据库与附件备份校验、前向迁移、原公司和管理员保留、目标版本健康及成功状态；日志 `/private/tmp/lean-ota-rehearsal-final.log`。
- Linux 隔离原生进程实际完成 Daphne/Nginx 受控停止与重启、PostgreSQL 15 备份、前向迁移、数据保留及任务完成；日志 `/private/tmp/lean-ota-native-rehearsal.log`。测试不挂 Docker socket，验证了非 root Nginx 临时目录。未声称完成 Windows/macOS 真机 OTA。
- 演练使用仅本地 v9.0.0 安装包及迁移夹具，替代远端下载流；官方资产校验另有单元测试。未发布该版本，也未改动 v1.0.0/v1.1.0 标签。一次性宿主机配置及故障恢复边界写在 README。
- 隔离端口 18360 上桌面与移动端完整浏览器回归 22 项通过，含六角色全链、附件、导入导出、BOM 多选和版本入口；日志 `/private/tmp/lean-ota-browser-final.log`。截图发现窄屏弹窗溢出后补充自适应宽度和边界断言，专项复测日志 `/private/tmp/lean-ota-dialog-final.log`。
