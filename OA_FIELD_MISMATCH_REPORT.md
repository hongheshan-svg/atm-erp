# OA模块前端字段与后端API不匹配检查报告
生成时间: 2026-01-30 08:00:39
================================================================================

## Meeting.vue
**状态**: ⚠️ 未找到对应的后端序列化器映射
**前端使用的字段**: attended, completed, end_time, in_progress, is_online, is_required, meeting_no, organizer_name, participant_count, response, response_display, room_name, start_time, status, status_display, title, user_name

## Attendance.vue
**状态**: ❌ 发现字段不匹配

### 问题字段列表
| 前端字段名 | 后端字段名 | 状态 |
|-----------|----------|------|
| `early_days` | - | ❌ 后端未提供 |
| `late_days` | - | ❌ 后端未提供 |
| `leave_days` | - | ❌ 后端未提供 |
| `total_days` | - | ❌ 后端未提供 |
| `total_overtime_hours` | - | ❌ 后端未提供 |
| `total_work_hours` | `work_hours` | ⚠️ 可能对应 |

### 建议修复方案
1. **后端未提供的字段**:
   - `early_days`: 检查后端序列化器是否需要添加此字段，或前端是否应该使用其他字段名
   - `late_days`: 检查后端序列化器是否需要添加此字段，或前端是否应该使用其他字段名
   - `leave_days`: 检查后端序列化器是否需要添加此字段，或前端是否应该使用其他字段名
   - `total_days`: 检查后端序列化器是否需要添加此字段，或前端是否应该使用其他字段名
   - `total_overtime_hours`: 检查后端序列化器是否需要添加此字段，或前端是否应该使用其他字段名
   - `total_work_hours`: 检查后端序列化器是否需要添加此字段，或前端是否应该使用其他字段名

2. **后端提供但前端未使用的字段**（参考）:
   - `id`
   - `user`

--------------------------------------------------------------------------------

## VehicleRequestList.vue
**状态**: ❌ 发现字段不匹配

### 问题字段列表
| 前端字段名 | 后端字段名 | 状态 |
|-----------|----------|------|
| `passengers` | - | ❌ 后端未提供 |
| `purpose_detail` | `purpose` | ⚠️ 可能对应 |
| `start_mileage` | - | ❌ 后端未提供 |

### 建议修复方案
1. **后端未提供的字段**:
   - `passengers`: 检查后端序列化器是否需要添加此字段，或前端是否应该使用其他字段名
   - `purpose_detail`: 检查后端序列化器是否需要添加此字段，或前端是否应该使用其他字段名
   - `start_mileage`: 检查后端序列化器是否需要添加此字段，或前端是否应该使用其他字段名

2. **后端提供但前端未使用的字段**（参考）:
   - `applicant_name`
   - `id`

--------------------------------------------------------------------------------

## MeetingList.vue
**状态**: ❌ 发现字段不匹配

### 问题字段列表
| 前端字段名 | 后端字段名 | 状态 |
|-----------|----------|------|
| `agenda` | - | ❌ 后端未提供 |
| `online_link` | - | ❌ 后端未提供 |
| `room` | `room_name` | ⚠️ 可能对应 |

### 建议修复方案
1. **后端未提供的字段**:
   - `agenda`: 检查后端序列化器是否需要添加此字段，或前端是否应该使用其他字段名
   - `online_link`: 检查后端序列化器是否需要添加此字段，或前端是否应该使用其他字段名
   - `room`: 检查后端序列化器是否需要添加此字段，或前端是否应该使用其他字段名

2. **后端提供但前端未使用的字段**（参考）:
   - `room_name`

--------------------------------------------------------------------------------

## LeaveList.vue
**状态**: ✅ 字段匹配正常


--------------------------------------------------------------------------------

## ScheduleList.vue
**状态**: ❌ 发现字段不匹配

### 问题字段列表
| 前端字段名 | 后端字段名 | 状态 |
|-----------|----------|------|
| `description` | - | ❌ 后端未提供 |
| `priority` | - | ❌ 后端未提供 |

### 建议修复方案
1. **后端未提供的字段**:
   - `description`: 检查后端序列化器是否需要添加此字段，或前端是否应该使用其他字段名
   - `priority`: 检查后端序列化器是否需要添加此字段，或前端是否应该使用其他字段名

2. **后端提供但前端未使用的字段**（参考）:
   - `is_public`
   - `type_display`

--------------------------------------------------------------------------------

## AnnouncementList.vue
**状态**: ❌ 发现字段不匹配

### 问题字段列表
| 前端字段名 | 后端字段名 | 状态 |
|-----------|----------|------|
| `content` | - | ❌ 后端未提供 |
| `target_all` | - | ❌ 后端未提供 |
| `top` | `is_top` | ⚠️ 可能对应 |
| `unread` | - | ❌ 后端未提供 |

### 建议修复方案
1. **后端未提供的字段**:
   - `content`: 检查后端序列化器是否需要添加此字段，或前端是否应该使用其他字段名
   - `target_all`: 检查后端序列化器是否需要添加此字段，或前端是否应该使用其他字段名
   - `top`: 检查后端序列化器是否需要添加此字段，或前端是否应该使用其他字段名
   - `unread`: 检查后端序列化器是否需要添加此字段，或前端是否应该使用其他字段名

2. **后端提供但前端未使用的字段**（参考）:
   - `id`
   - `is_active`
   - `priority_display`
   - `status`
   - `status_display`

--------------------------------------------------------------------------------

## AttendanceImport.vue
**状态**: ⚠️ 未找到对应的后端序列化器映射
**前端使用的字段**: date, employee_id, employee_name, error, import_status, import_time, includes, late_minutes, leave_hours, leave_type, month, operator, overtime_hours, remark, skip, status, status_text, success_count, total_count, valid

## VehicleList.vue
**状态**: ❌ 发现字段不匹配

### 问题字段列表
| 前端字段名 | 后端字段名 | 状态 |
|-----------|----------|------|
| `annual_inspection_date` | - | ❌ 后端未提供 |
| `engine_no` | - | ❌ 后端未提供 |
| `insurance_company` | - | ❌ 后端未提供 |
| `insurance_expire_date` | - | ❌ 后端未提供 |
| `next_inspection_date` | - | ❌ 后端未提供 |
| `notes` | - | ❌ 后端未提供 |
| `vin` | - | ❌ 后端未提供 |

### 建议修复方案
1. **后端未提供的字段**:
   - `annual_inspection_date`: 检查后端序列化器是否需要添加此字段，或前端是否应该使用其他字段名
   - `engine_no`: 检查后端序列化器是否需要添加此字段，或前端是否应该使用其他字段名
   - `insurance_company`: 检查后端序列化器是否需要添加此字段，或前端是否应该使用其他字段名
   - `insurance_expire_date`: 检查后端序列化器是否需要添加此字段，或前端是否应该使用其他字段名
   - `next_inspection_date`: 检查后端序列化器是否需要添加此字段，或前端是否应该使用其他字段名
   - `notes`: 检查后端序列化器是否需要添加此字段，或前端是否应该使用其他字段名
   - `vin`: 检查后端序列化器是否需要添加此字段，或前端是否应该使用其他字段名

2. **后端提供但前端未使用的字段**（参考）:
   - `id`

--------------------------------------------------------------------------------

## Schedule.vue
**状态**: ⚠️ 未找到对应的后端序列化器映射
**前端使用的字段**: all_day, color, location, start_time, title

## IMChat.vue
**状态**: ⚠️ 未找到对应的后端序列化器映射
**前端使用的字段**: active, my, role, username

## AssetList.vue
**状态**: ❌ 发现字段不匹配

### 问题字段列表
| 前端字段名 | 后端字段名 | 状态 |
|-----------|----------|------|
| `depreciation` | - | ❌ 后端未提供 |
| `depreciation_years` | - | ❌ 后端未提供 |
| `notes` | - | ❌ 后端未提供 |
| `purchase_date` | - | ❌ 后端未提供 |
| `residual_rate` | `id` | ⚠️ 可能对应 |
| `search` | - | ❌ 后端未提供 |
| `serial_no` | - | ❌ 后端未提供 |
| `specification` | - | ❌ 后端未提供 |
| `total_count` | - | ❌ 后端未提供 |
| `total_current_value` | `current_value` | ⚠️ 可能对应 |
| `total_purchase_value` | - | ❌ 后端未提供 |
| `warranty_expire_date` | - | ❌ 后端未提供 |

### 建议修复方案
1. **后端未提供的字段**:
   - `depreciation`: 检查后端序列化器是否需要添加此字段，或前端是否应该使用其他字段名
   - `depreciation_years`: 检查后端序列化器是否需要添加此字段，或前端是否应该使用其他字段名
   - `notes`: 检查后端序列化器是否需要添加此字段，或前端是否应该使用其他字段名
   - `purchase_date`: 检查后端序列化器是否需要添加此字段，或前端是否应该使用其他字段名
   - `residual_rate`: 检查后端序列化器是否需要添加此字段，或前端是否应该使用其他字段名
   - `search`: 检查后端序列化器是否需要添加此字段，或前端是否应该使用其他字段名
   - `serial_no`: 检查后端序列化器是否需要添加此字段，或前端是否应该使用其他字段名
   - `specification`: 检查后端序列化器是否需要添加此字段，或前端是否应该使用其他字段名
   - `total_count`: 检查后端序列化器是否需要添加此字段，或前端是否应该使用其他字段名
   - `total_current_value`: 检查后端序列化器是否需要添加此字段，或前端是否应该使用其他字段名
   - `total_purchase_value`: 检查后端序列化器是否需要添加此字段，或前端是否应该使用其他字段名
   - `warranty_expire_date`: 检查后端序列化器是否需要添加此字段，或前端是否应该使用其他字段名

2. **后端提供但前端未使用的字段**（参考）:
   - `current_user`
   - `id`

--------------------------------------------------------------------------------


## 总结
- 检查文件数: 12
- 发现不匹配: 7
- 正常匹配: 1
