#!/usr/bin/env bash
set -euo pipefail
ROOT="$(git rev-parse --show-toplevel)"
bash "$ROOT/scripts/precheck-tests.sh" --all
(cd "$ROOT/frontend" && npm run lint && npm run typecheck && npm run test && npm run build)
