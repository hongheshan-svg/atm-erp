# Lean ERP 概念图实施验收

## 本轮：BOM 选料与采购草稿同屏（2026-09-12）

按用户最新决定取消折叠，移除折叠按钮、状态和布局分支，恢复采购草稿持续显示；桌面左右排列，移动端上下排列。布局、键盘纠错及单次保存双端2项复验通过，记录 `/private/tmp/erp-bom-always-visible-e2e`；已查看[桌面采购](/Users/zhengshan/.codex/visualizations/2026/09/08/01a0832c-3411-7620-83b4-bdd8ceec3d12/BOM-purchase-20260912/桌面采购.png)，六行BOM、三行草稿及保存按钮同屏，无可见遮挡或溢出。正常桌面和移动截图同步更新；早期折叠截图仅为历史记录。

final result: passed

- 目标：最近一轮第3张图的左右操作布局；用户要求保持现有系统风格，因此沿用深色导航、蓝色主操作、原字体、P标志和组件，不把示意图的浅色导航替换到全系统。
- 源图：`/Users/zhengshan/.codex/generated_images/01a0832c-3411-7620-83b4-bdd8ceec3d12/exec-5fbd793a-5a78-4918-8fdd-1bd82b093092.png`。
- 实现：独立安装 `http://127.0.0.1:18508/erp/purchases`，进入“从 BOM 多选下单”。[最终桌面截图](/Users/zhengshan/.codex/visualizations/2026/09/08/01a0832c-3411-7620-83b4-bdd8ceec3d12/BOM-purchase-20260912/桌面采购.png)。
- 尺寸：源图1487×1058像素；r4桌面1488×1056，最终r5按源图调整为1487×1058像素与CSS视口，deviceScaleFactor=1，无密度缩放。另验390×844移动视口；[移动选料](/Users/zhengshan/.codex/visualizations/2026/09/08/01a0832c-3411-7620-83b4-bdd8ceec3d12/BOM-purchase-20260912/移动选料.png)、[移动草稿](/Users/zhengshan/.codex/visualizations/2026/09/08/01a0832c-3411-7620-83b4-bdd8ceec3d12/BOM-purchase-20260912/移动草稿.png)。
- 对比状态：管理员，六行物料，选择伺服电机/导轨/气缸三行，数量4/8/2，价格1500/260/180，总额8440.00，月结30。实际项目/编码由业务接口生成；实际夹具库存为零并用一条停用物料验证禁选，与概念图的共享库存样例不同，不为像素匹配伪造业务数据。
- 全图证据：在同一次图像输入中以原始尺寸同时打开源图、最终桌面截图和移动草稿截图，检查布局与细节。原图全尺寸可清楚读取各表头、数量、金额、按钮和选中状态，因此未另裁切或重采样图片；重点逐项检查了左表首末行、右侧三行输入/合计/保存区，以及左下角说明。

### 问题与修复记录

| 级别 | 先前证据 | 修复与最终证据 |
| --- | --- | --- |
| P2 | 初版重复页面标题和展开的项目选择挤占高度；三行草稿使底部操作落到视口外（`erp-bom-purchase-ui-e2e-r2`） | 进入工作区隐藏重复页标题，选好项目后折叠；草稿按视口剩余高度设置内部滚动，保存区常驻。r4三行与按钮均完整可见。 |
| P2 | 初版草稿逐条上下排列过高，输入最后一项后首项标题被内部滚动裁切（`erp-bom-layout-desktop-r1`） | 桌面每行改为物料与数量/价格/小计横排；最终截图归顶后三项全见，保留长列表滚动。 |
| P2 | 六行BOM超过原520px表格上限，第六行只显示一部分（`erp-bom-layout-r3`） | 降低局部行间距并将表格上限改为560px；r4首末行同时可见断言通过，截图六行完整。 |
| P2 | “仅看缺料”数量排除停用行，而筛选包含该行 | 标记按真实缺料行数统计，停用仅影响勾选资格；最终六条缺料、停用项禁选，与显示一致。 |

### 五项视觉检查

- 字体：继承现有字体，页面标题22px、草稿19px、主内容13px，辅助编码11～12px。信息层级明确，长编码可换行，不使用截图文字代替控件。
- 布局：桌面左右约1.65:1，右侧独立滚动，六行示例与保存动作在最终1487×1058视口内可见；窄屏上下排列，宽表仅在表内横向滚动。未发现遮挡、整页横向溢出或不可达控件。
- 颜色：使用现有主色与边框，选中浅蓝、缺料琥珀色、错误红色；深色侧栏按用户约束保留，底部说明固定在左下。
- 素材：复用原P标志和Element Plus图标。该操作页没有新增照片、插图或位图资产；不把概念图当背景图。
- 文案：保存动作明确为采购草稿，显示共享库存、月底账期、失效选择和含税口径；额外编码类别、备注与明细交期保留实际功能，不添加概念标记或演示数据到产品代码。

### 交互证据与边界

- 最终布局用例r4桌面、移动2项通过；r5按源图1487×1058补拍桌面，同一用例再次通过，不重复计数。跨页筛选保存双端2项通过。Space勾选、Tab转移、Enter提交、零数量前端纠错、单次POST、稳定BOM行关联、项目入口预选和返回焦点均验证。
- 浏览器fixture监听pageerror及console warning/error，最终无未预期异常。内嵌浏览器控制面未连接；使用仓库已授权的隔离Chromium测试与真实浏览器截图验证，预览入口已交给Codex面板打开队列，不声称人工浏览器会话已打开。
- 未留下需修复的P0/P1/P2。视觉细化可继续基于用户反馈进行；未运行本次范围外的发布全链条、手机真机键盘或企业实际试运行。

## 历史验收记录（本轮以前）

状态：通过。本轮 UI 实施范围内无未解决的 P0/P1/P2 问题。

参考源：当前任务的《ERP-UI概念图索引.md》所列 14 张选定概念图。桌面基准为 1487 × 1058，移动端为 390 × 844。使用隔离真实业务数据与真实接口，不把图中的演示计数写入产品。

## 已核对的视觉问题与处理

| 问题 | 处理 | 验证 |
| --- | --- | --- |
| 详情从页面中部展开，底部操作落到屏幕外 | 根据详情顶部与可视高度计算内部滚动区，保留操作区 | 月结、对账和长表单桌面/移动验证已通过 |
| 项目选择的搜索按钮挤成两行 | 固定按钮不收缩，输入框允许收缩，宽屏横排 | 最终 BOM 桌面、手机及中间宽度截图通过 |
| 登录图像有矩形背景边界，标题比例偏小 | 调整图像边缘融合、尺寸和标题层级 | 最终登录截图与参考图对比通过 |
| 报表指标过高，表格开始位置偏下 | 收紧筛选和指标间距，保留金额口径说明 | 最终报表截图通过 |
| 合同操作分散在顶部 | 版本、归档与打印归入右侧操作栏，窄屏转为正文上方 | 最终截图、合同归档与打印回归通过 |
| 用户表把管理员报表权限显示为未授权 | 明确显示“管理员默认”，经理仍显示显式授权值 | 已核对截图 |
| 审计长详情挤在半列中 | 只读详情占满侧栏宽度并自动换行 | 最终 audit-details 截图及关闭操作通过 |

## 逐页范围

1. 工作台：指标、优先处理表、分类入口、各类待办与分页。
2. 经营报表：筛选、经营指标、关注条件、项目明细和只读关注视图。
3. 销售：状态与搜索、合同字段、侧边详情、签约与技术附件。
4. 项目：列表、状态筛选、项目入口。
5. 项目详情：任务与工时、BOM 两页签、采购、交付与售后、款项/对账/流水、预算与实际成本、附件。
6. BOM：项目选择、品牌/单元/类别/缺料组合筛选、多选采购、模板和变更。
7. 采购：状态、明细、收货、审批、合同与附件。
8. 合同：版本、正文与完整附件、整套打印和归档。
9. 库存：现有库存、流水、规格品牌、数量、领料与盘点。
10. 收付款：款项、业务对账、月结、收付流水、银行到账与认领。
11. 基础资料：物料、客户供应商、自定义编码、侧边表单。
12. 设置：用户及兼岗、公司、编号、审计、账户。
13. 登录：行业插图、账号密码、显示密码、错误与提交状态。
14. 首次安装：管理员、公司、人员、编号、确认启用；已安装指南。

## 视觉和业务适配原则

- 字体、深色侧栏、浅色画布、蓝色主动作、状态标签、卡片边框和侧边详情使用统一规则。
- 概念图的静态样例与真实数据不同；授权菜单、真实字段、含税经营口径和金融纠错流程不为匹配假数据而省略。
- 详情默认由用户打开，列表不会自动选中一张业务单据。窄屏详情在同页展示，表格在自身区域横向滚动。
- 合同正文沿用原业务条款和 A4 打印规则，打印隐藏系统导航与操作栏。
- 每份对比同时读取参考图与实现截图，检查层级、间距、字体、颜色、素材及内容；截图须等待数据加载完成。

## 检查证据

- 后端完整检查：248 项通过，日志 `/private/tmp/erp-ui-backend-tests.log`。
- 前端最终版本：lint、typecheck、60 项单元测试及构建通过。
- 全新独立安装：桌面和手机各一个独立数据库，五步向导、改密、公司、兼岗用户与编号启用通过，日志 `/private/tmp/erp-ui-first-verified.log`、`/private/tmp/erp-ui-first-mobile-verified.log`。
- 完整浏览器回归：40 项通过，2 项新安装专项按设计跳过并由上述独立安装覆盖，日志 `/private/tmp/erp-ui-acceptance.log`。
- 最后审计详情修复后全部页面专项：桌面、手机 2 项通过，含 768px/1024px 中间宽度，日志 `/private/tmp/erp-ui-final-capture.log`。
- [108 张最终原始截图索引](/Users/zhengshan/.codex/visualizations/2026/09/08/01a0832c-3411-7620-83b4-bdd8ceec3d12/UI-implemented-20260910/截图索引.md)：覆盖 14 类页面、各子模块、关键表单与五步安装。

## 最终设计检查

- 字体与层级：统一标题、表头、正文和辅助说明；登录标题与插图比例已对照概念图调整。
- 布局与响应：216px 深色侧栏、底部说明、统一工具栏与卡片间距；390px、768px、1024px 和桌面布局无整页横向溢出，宽表格保留内部滚动。
- 颜色与素材：统一蓝色主操作及语义状态标签，插图保持原比例并处理背景边缘，图标沿用 Element Plus。
- 文案与内容：使用真实授权数据与业务口径；保留合同条款、完整字段和审计原始事实，未为贴合概念图伪造数据。
- 交互与可访问性：导航、筛选、分页、页签、详情关闭、附件、表单与主业务链通过；键盘焦点返回、密码显示、长表单底部操作和加载状态已检查。
- 本次验证使用隔离 Chromium 和模拟视口，不代表物理手机软键盘、其他浏览器或企业实际试运行验收。
